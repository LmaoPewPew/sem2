package sn.prog2.exercises.set07;

public class MyDequeUtil {

    public static <T> MyDeque<T> merge(Mydeque<T> d1, Mydeque<T> d2) {
        MyDeque<T> deque = new MyDeque<>(d1.size() + d2.size());
        for (int i = 0; i < d1.; i++) {
        }
    }
}
